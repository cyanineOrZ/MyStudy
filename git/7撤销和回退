git reset 用于回退版本 默认为mixed模式


git reset --soft
soft将会把该文件回退到某一个版本，并保存该文件在工作区和暂存区的修改内容，也即不会删除该两个区域的痕迹
也就是，不会删除在该回退到的版本后的文件，也不会删除暂存区的内容


git reset --hard
hard将会把该文件回退到某一个版本，并且不保存全部修改内容（慎用）
直接把新文件以及其他的如暂存区的内容全部删除


gir reset --mixed(default)
mixed将会保存工作区的内容，而不保存暂存区的内容